사실 알고리즘이랄 건 없고,
계란의 수가 한정되어있으므로 
일단 1000개밖에 안되므로 소비자의 살수있는 금액을 정렬한 후
각 인덱스일때의 수익을 계산한다. 이때 살수있는 계란의 수가
한정된 전체 n보다 크면 n까지만 사고, 아니면 살수있는 계란수 p개를 산다.
마지막에 hashmap 사용해서 최대 수익일떄의 계란 값을 찾아내면 된다.
Entry<> entryset : map.entrySet() 써서
entryset.getKey(), getValue() 쓰면 맵 다루기 편하다.