* 연속하는 갯수를 구할때 간혹 내자신과 내 뒤를 체크하려는
잘못된 알고리즘 버릇이 존재.

-> 해결:

한번 loop 차례에 나말고 내 뒤나 앞을 함부로 호출하지말것.

그냥 현재 값위치가 .인지 x인지를 체크해서
.이 맞으면 seat++ 해주고.
x이면 앉을수 없는자리이다. -> 이경우 2가지로 나뉜다.
지금까지의 가능한 자리들의 갯수가 몇개인지

if(x이면)
 if(seat >=2) //앉기 가능
     ans++; //정답 추가
 그리고 나면 다시 seat은 초기화해줘야한다.
 seat = 0

이 루프가 다 돌고 나면
마지막에는 벽이 존재한다. loop의 마지막은 다시 다음줄로 가니까
그때에도 앉을자리를 체크해줘야한다.

즉
for(int i 0 n ++)
	for(int j 0 n ++){
		if(. ) seat++
		else
			if(seat >=2) ans++, seat =0
	//이때가 벽을 만난경우
	if(seat>=2) 벽 전까지 2이상이면 
	ans++
