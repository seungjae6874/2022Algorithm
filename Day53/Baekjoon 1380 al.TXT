//해당 문제는 hashmap을 적절히 잘 사용하면 쉽게 풀수 있다.
//또한 A와 B를 둘다 받음이 사실 문제를 더 쉽게 만든다.
//A와 B배열을 2개 만들어놓고, 값을 입력받으면 
A가 들어오면 A의 index+1번째 배열값에 1을
B가 들어오면 B의 index+1번째 배열값에 1을 넣는다.

//사전에 전체 맵(넘버, 학생이름)
//귀걸이 못받은 학생맵(CNT,학생이름)으로 만들어놓고

A는 1이고 B는 1이 아니면
 학생이름을 귀걸이못받은 학생맵에 put(++cnt, 학생이름)
B는 1이고 A는 1이 
 학생이름을 귀걸이못받은 학생맵에 put(++cnt, 학생이름)

을 통해서 마지막에 N이 0이 들어오면 
귀걸이 못받은 학생맵을 Map.EntrySET<T1, T2> entrymap : map.entrySet()
사용해서 전체 출력후 getKey(), getValue()

break를 통해 while문 탈출