해당 문제는 문자열들을 담아서 반복문을 통해
나와 다른 문자열들과의 다른 문자가 존재하는 부분을 찾아내는 것이
핵심이다
for문으로 나와 그 다음 문자열들을 비교해서 charAt을 통해
다른 위치를 찾아내면? ans라는 문자열에 해당 위치 값을 ? 로 변경

이때는 substring(startindx, endidx)를 통해 + ? + sustring(endidx+1)
처리하면 된다.
2개 인자를 받을 경우는 endidx -1까지가 담긴다.