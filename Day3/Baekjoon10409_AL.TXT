1. 입력받은 시간할당값들을 배열에 담는다.
2. 배열을 돌면서 
만약 이전값들까지의 합에 현재인덱스값을 더했을 때 T보다 크다면
PRINT횟수 후 BREAK (탈출해야한다)
3. 만약 합이 T보다 작거나 같다면 계속 SUM해준다.
** 위의 처리는 코너케이스가 존재하는데 바로 배열 끝까지 갔을때
모든 합이 T보다 작거나 같으면 CNT를 출력하지 않는다는 것이다.
** 따라서 마지막에 CNT와 N이 같고(배열의 모든 할당값들을 사용할 수 있고,
CSUM이 T보다 작거나 같으면) 그대로 배열의 길이 N을 출력하면 됨.